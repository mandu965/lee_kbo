---
title: "{{ title }}"
category: KBO 경기 분석
tags: [KBO, 야구, 승리예측, AI분석, 오늘경기, KBO예측]
---

# {{ title }}

{{ intro }}

---

## {{ summary_header }} — {{ date_kr }}

| 경기 | 예측 승자 | 홈 승률 | 원정 승률 | 신뢰도 |
|------|----------|---------|---------|--------|
{% for g in games %}| {{ g.home_team }} vs {{ g.away_team }} | **{{ g.predicted_winner }}** | {{ g.home_prob }}% | {{ g.away_prob }}% | {{ g.confidence_level }}{% if g.data_completeness < 80 %} ⚠{% endif %} |
{% endfor %}

> 예측 확률은 AI 통계 모델 기반이며 실제 결과를 보장하지 않습니다.

---

## {{ analysis_header }}

{% for g in games %}
### {{ g.home_team }} vs {{ g.away_team }}

**경기 정보** : {{ g.game_date }} {{ g.start_time_str }} / {{ g.stadium }}

#### 선발 투수

| 구분 | 투수 | ERA |
|------|------|-----|
| 홈 ({{ g.home_team }}) | {% if g.home_starter != "미정" %}{{ g.home_starter }}{% else %}{{ g.starter_unknown_phrase }}{% endif %} | {% if g.home_starter_era is not none %}{{ g.home_starter_era }}{% else %} — {% endif %} |
| 원정 ({{ g.away_team }}) | {% if g.away_starter != "미정" %}{{ g.away_starter }}{% else %}{{ g.starter_unknown_phrase }}{% endif %} | {% if g.away_starter_era is not none %}{{ g.away_starter_era }}{% else %} — {% endif %} |

{% if g.home_starter == "미정" or g.away_starter == "미정" %}
> ⚠️ **{{ g.low_data_note }}**
{% endif %}

#### 최근 흐름

| 팀 | 최근 5경기 | 요약 |
|----|-----------|------|
| **{{ g.home_team }}** | `{{ g.home_form }}` | {{ g.home_form_kr }} |
| **{{ g.away_team }}** | `{{ g.away_form }}` | {{ g.away_form_kr }} |

#### AI 핵심 요인

{% if g.key_factors %}{% for factor in g.key_factors %}
- {{ factor }}
{% endfor %}{% else %}
- ELO 전력 기반 분석 (선발 투수 및 추가 데이터 반영 전)
- 최근 흐름: {{ g.home_team }} {{ g.home_form_kr }} / {{ g.away_team }} {{ g.away_form_kr }}
{% endif %}

#### 예측 결과

> **{{ g.predicted_winner }}** 승리 예측 — **{{ g.predicted_winner_prob }}%** ({{ g.prob_label }})
>
> 데이터 완전성: {{ g.data_completeness }}% / 신뢰도: **{{ g.confidence_level }}**

---
{% endfor %}

## AI 예측 모델 안내

| 요소 | 가중치 |
|------|--------|
| ELO 전력 레이팅 | 40% |
| 선발 투수 지표 | 28% |
| 최근 10경기 흐름 | 14% |
| 홈 이점 | 8% |
| 파크팩터·날씨·불펜 | 10% |

선발 투수 미확정 경기는 ELO 기반 초안 예측입니다. 라인업 확정 후 자동 업데이트됩니다.

## 이번 달 모델 적중률

- 적중: **{{ monthly_correct }}경기** / 전체: **{{ monthly_total }}경기**
- 적중률: **{{ monthly_accuracy }}%**

{{ outro }}

---

## KBO AI 예측 시스템

더 자세한 경기 분석과 실시간 예측은 아래에서 확인할 수 있습니다.

👉 [KBO Predictor — AI 경기 예측](https://lee-kbo.onrender.com/)

---

> **면책 고지**: 본 예측은 통계 모델 기반 참고 정보이며, 실제 경기 결과와 다를 수 있습니다. 스포츠 베팅 등 금전적 결정의 근거로 사용하지 마십시오.
